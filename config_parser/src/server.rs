use crate::config::ServerConfig;

/// Stub for Member 5: Configuration Parser work.
/// This lets tests compile without integrating other modules yet.
pub fn start(_config: &ServerConfig) {
    // no-op: full server logic added later
}
